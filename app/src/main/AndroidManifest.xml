<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yoavbear.homey">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChoreCreator"
            android:label="@string/title_activity_chore_creator"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ChoreUpdater">
            android:theme="@style/AppTheme.NoActionBar" />
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".FamilyMemberLoginActivity" />
        <activity android:name=".AddFamilyMemberActivity" />
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".CompletedChoresActivity" />
        <activity android:name=".ShoppingListActivity" />
        <activity android:name=".ShoppingListItemCreator"></activity>
    </application>

</manifest>